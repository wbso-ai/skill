name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check-cli-sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify skill CLI scripts are synced
        run: bin/check-wbso-cli-sync
